{
  "name": "astrobuild-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx -y concurrently --kill-others -n \"backend,frontend\" -c \"blue,green\" \".\\.venv\\Scripts\\python -m uvicorn backend.main:app --port 8000 --reload\" \"npm run dev --prefix frontend\""
  }
}
